#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    int H, W;
    cin >> H >> W;
    string c[H];

    for(int i = 0; i < H; ++i){
        cin >> c[i];
    }

    for(int i = 0; i < H; ++i){
        cout << c[i] << endl;
        cout << c[i] << endl;
    }
    return 0;
}
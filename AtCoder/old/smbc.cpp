#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    int m, d, M, D;
    cin >> m >> d >> M >> D;
    if(m != M) cout << 1 << endl;
    else cout << 0 << endl;
}
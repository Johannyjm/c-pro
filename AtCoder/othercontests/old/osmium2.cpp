#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int N, K;
    cin >> N >> K;
    int now = 2019;
    int cand[N];

    for(int i = 0; i < N; ++i) cin >> cand[i];

    sort(cand, cand + N);
    int max

    return 0;
}

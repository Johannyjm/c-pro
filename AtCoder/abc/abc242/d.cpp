#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); ++i)
#define rep1(i, n) for(int i = 1; i < (n); ++i)
using namespace std;
using ll = long long;

string s;
int n;

void solve(ll t, ll k){
    
}


int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    cin >> s;
    n = s.size();

    int q;
    cin >> q;
    while(q--){
        ll t, k;
        cin >> t >> k;

        solve(t, k);
    }

    return 0;
}
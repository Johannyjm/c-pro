#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    ll N, K;
    cin >> N >> K;

    printf("%.0f\n", K * pow((K - 1), N - 1));
    //cout << "\n";

}